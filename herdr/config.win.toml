# Herdr keybindings modeled after ~/.tmux.conf (Windows version).
onboarding = false

[terminal]
new_cwd = "follow"

[keys]
prefix = "alt+o"

# Help / reload / navigation surfaces
help = "prefix+?"
reload_config = "prefix+enter"
workspace_picker = "prefix+tab"
goto = "prefix+space"
detach = "prefix+d"
open_notification_target = "alt+u"

# Panes: Alt+[ / Alt+] split, Alt+Shift+H/J/K/L focus, Alt+\ closes.
focus_pane_left = ["prefix+h", "shift+left", "alt+H"]
focus_pane_down = ["prefix+j", "shift+down", "alt+J"]
focus_pane_up = ["prefix+k", "shift+up", "alt+K"]
focus_pane_right = ["prefix+l", "shift+right", "alt+L"]
edit_scrollback = "alt+U"
split_vertical = "alt+]"
split_horizontal = "alt+["
close_pane = "alt+backslash"
swap_pane_left = "prefix+alt+H"
swap_pane_down = "prefix+alt+J"
swap_pane_up = "prefix+alt+K"
swap_pane_right = "prefix+alt+L"
last_pane = "prefix+alt+."
resize_mode = "prefix+r"
resize_pane_left = "prefix+alt+h"
resize_pane_down = "prefix+alt+j"
resize_pane_up = "prefix+alt+k"
resize_pane_right = "prefix+alt+l"
zoom = "prefix+z"

# Tabs: tmux windows mapped to Herdr tabs
new_tab = "alt+O"
previous_tab = "alt+P"
next_tab = "alt+N"
switch_tab = "prefix+1..9"
close_tab = "prefix+alt+q"
rename_tab = "prefix+alt+r"
move_tab_previous = "prefix+alt+p"
move_tab_next = "prefix+alt+n"

# Workspaces: tmux sessions mapped to Herdr workspaces
rename_workspace = "prefix+alt+s"
new_workspace = "prefix+shift+n"
close_workspace = "prefix+shift+d"
next_workspace = "ctrl+backtick"
previous_workspace = "ctrl+~"

# Agents
next_agent = "alt+backtick"
previous_agent = "alt+~"

# Alt+| enters copy mode. Herdr has no tmux choose-buffer action for Alt+".
copy_mode = "alt+|"

# Navigate mode uses vi keys like tmux copy/select movement.
navigate_workspace_up = "up"
navigate_workspace_down = "down"
navigate_pane_left = "h"
navigate_pane_down = "j"
navigate_pane_up = "k"
navigate_pane_right = "l"

[ui]
tab_bar_position = "bottom"
tab_bar_right = [
  { type = "datetime", format = "%Y-%b-%d" },
  { type = "datetime", format = "%H:%M" },
]
tab_bar_right_separator = " | "
copy_on_select = true
confirm_close = true
prompt_new_tab_name = false
pane_borders = true
pane_gaps = false
mouse_capture = true
