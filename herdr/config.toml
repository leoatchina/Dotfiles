# Herdr keybindings modeled after ~/.tmux.conf.
onboarding = false

[terminal]
new_cwd = "follow"

[keys]
prefix = "alt+o"

# Help / reload / navigation surfaces
help = "prefix+?"
reload_config = "prefix+enter"
workspace_picker = "prefix+tab"
goto = "prefix+space"
detach = "prefix+d"

# Panes: Alt+[ / Alt+] split, Alt+Shift+H/J/K/L focus, Alt+\ closes.
split_vertical = "alt+]"
split_horizontal = "alt+["
focus_pane_left = "alt+shift+h"
focus_pane_down = "alt+shift+j"
focus_pane_up = "alt+shift+k"
focus_pane_right = "alt+shift+l"
close_pane = "alt+backslash"
swap_pane_down = "prefix+["
swap_pane_up = "prefix+]"
last_pane = "prefix+alt+."
resize_mode = "prefix+r"
zoom = "prefix+z"

# Tabs: tmux windows mapped to Herdr tabs
new_tab = "alt+shift+o"
previous_tab = "alt+shift+p"
next_tab = "alt+shift+n"
switch_tab = "prefix+1..9"
close_tab = "prefix+alt+q"
rename_tab = "prefix+alt+r"

# Workspaces: tmux sessions mapped to Herdr workspaces
rename_workspace = "prefix+alt+s"
new_workspace = "prefix+shift+n"
close_workspace = "prefix+shift+d"
next_workspace = "alt+backtick"
previous_workspace = "alt+shift+backtick"

# Alt+| enters copy mode. Herdr has no tmux choose-buffer action for Alt+".
copy_mode = "alt+shift+backslash"

# Navigate mode uses vi keys like tmux copy/select movement.
navigate_workspace_up = "up"
navigate_workspace_down = "down"
navigate_pane_left = "h"
navigate_pane_down = "j"
navigate_pane_up = "k"
navigate_pane_right = "l"

[[keys.command]]
key = "prefix+alt+p"
command = "$HOME/Dotfiles/herdr/move-tab.sh previous"
description = "move tab left"

[[keys.command]]
key = "prefix+alt+n"
command = "$HOME/Dotfiles/herdr/move-tab.sh next"
description = "move tab right"

[ui]
copy_on_select = true
confirm_close = true
prompt_new_tab_name = false
pane_borders = true
mouse_capture = true
